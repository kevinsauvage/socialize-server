const config = {
  cors: [
    'http://localhost:3000',
    'http://localhost:3001',
    'http://localhost:3002',
    //undefined,// uncoment to try the api on the browser
  ],
}
module.exports = config
